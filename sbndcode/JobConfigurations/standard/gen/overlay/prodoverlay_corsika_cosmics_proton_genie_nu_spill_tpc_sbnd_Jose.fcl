# Generates GENIE events together with CORSIKA cosmic rays.
# GENIE events are generated from the BNB beam,
# simulating the beam spill structure, inside the TPC volume
# (with 10 cm padding on each side).
# CORSIKA cosmics are generated using the
# "proton" model. 

#include "prodoverlay_corsika_cosmics_proton_genie_nu_spill_gsimple-configk-v1_tpc.fcl"

physics.producers.corsika.SampleTime:110.2e-6
physics.producers.corsika.TimeOffset:-10.7e-6
